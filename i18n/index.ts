import { DEFAULT_LOCALE, SUPPORTED_LOCALES } from './config/settings';

// 共通の設定をエクスポート
export { DEFAULT_LOCALE, SUPPORTED_LOCALES };

// ロケール関連のユーティリティ関数をエクスポート
export { GET_LOCALE_FROM_URL, IS_VALID_LOCALE, CREATE_LOCALIZED_URL } from './config/settings';
